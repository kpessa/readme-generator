let testData = {
  title: 'readme-generator',
  description: 'creates a README file for a new project',
  installation:
    'You first need to install node.js.  After installing node.js, clone or copy the repository.  Once cloned, in the terminal, type <code>npm install</code> to install the development dependencies (ie. inquirer)',
};

module.exports = testData;
